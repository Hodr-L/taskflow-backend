# ============================================
# TaskFlow 后端环境变量配置模板
# 复制此文件为 .env 并修改配置值
# ============================================

# 应用配置
APP_ENV=development                    # development, testing, production
APP_NAME=TaskFlow
APP_VERSION=1.0.0
DEBUG=true                            # 生产环境设为 false

# 服务器配置
SERVER_HOST=0.0.0.0
SERVER_PORT=30088
SERVER_READ_TIMEOUT=10
SERVER_WRITE_TIMEOUT=10
SERVER_IDLE_TIMEOUT=60

# MySQL数据库配置（使用 DB_ 前缀，与 Docker Compose 一致）
DB_HOST=localhost                     # 容器内使用 mysql
DB_PORT=53306                        # 主机端口（容器内为3306）
DB_NAME=taskflow
DB_USER=taskflow
DB_PASSWORD=taskflow123              # ⚠️ 生产环境必须修改
DB_CHARSET=utf8mb4
DB_PARSE_TIME=true
DB_LOC=Local
DB_MAX_OPEN_CONNS=25
DB_MAX_IDLE_CONNS=5
DB_CONN_MAX_LIFETIME=300

# Redis配置
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=redis123              # ⚠️ 生产环境必须修改
REDIS_DB=0
REDIS_POOL_SIZE=10
REDIS_MIN_IDLE_CONNS=2

# JWT配置
JWT_SECRET=your-super-secure-jwt-secret-change-in-production  # ⚠️ 必须修改，长度至少32位
JWT_ACCESS_EXPIRE_HOURS=24           # 访问令牌有效期（小时）
JWT_REFRESH_EXPIRE_DAYS=7            # 刷新令牌有效期（天）

# Kafka配置（开发环境可禁用）
KAFKA_ENABLED=false                  # 开发时可设为 false
KAFKA_BROKERS=localhost:9092        # 容器内使用 kafka:29092
KAFKA_CLIENT_ID=taskflow-backend
KAFKA_NOTIFICATION_GROUP_ID=notification-consumer-group
KAFKA_AUDIT_GROUP_ID=audit-consumer-group

# 文件上传配置
UPLOAD_MAX_SIZE=10485760             # 10MB
UPLOAD_STORAGE_PATH=./uploads
UPLOAD_ALLOWED_TYPES=image/jpeg,image/png,image/gif,application/pdf

# 日志配置
LOG_LEVEL=info                       # debug, info, warn, error
LOG_FORMAT=json                      # json, text
LOG_OUTPUT=stdout                    # stdout, file

# CORS配置
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080
CORS_ALLOWED_METHODS=GET,POST,PUT,PATCH,DELETE,OPTIONS
CORS_ALLOWED_HEADERS=Origin,Content-Type,Accept,Authorization,X-Requested-With
CORS_ALLOW_CREDENTIALS=true
CORS_MAX_AGE=86400

# 邮件配置（用于通知，可选）
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=noreply@taskflow.com
SMTP_TLS=true

# 监控配置（可选）
PROMETHEUS_ENABLED=false             # 生产环境可启用
PROMETHEUS_PORT=9090
HEALTH_CHECK_ENABLED=true
HEALTH_CHECK_PORT=8081

# 速率限制（可选）
RATE_LIMIT_ENABLED=true
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=60                 # 秒

# 功能开关（可选）
FEATURE_NOTIFICATIONS_ENABLED=true
FEATURE_AUDIT_LOG_ENABLED=true
FEATURE_FILE_UPLOAD_ENABLED=true
FEATURE_REAL_TIME_UPDATES_ENABLED=true

# ============================================
# 以下为兼容性配置（旧前缀）
# ============================================
# 数据库兼容配置（支持旧 DATABASE_ 前缀）
DATABASE_HOST=${DB_HOST}
DATABASE_PORT=${DB_PORT}
DATABASE_NAME=${DB_NAME}
DATABASE_USER=${DB_USER}
DATABASE_PASSWORD=${DB_PASSWORD}
DATABASE_CHARSET=${DB_CHARSET}
DATABASE_PARSE_TIME=${DB_PARSE_TIME}
DATABASE_LOC=${DB_LOC}
DATABASE_MAX_OPEN_CONNS=${DB_MAX_OPEN_CONNS}
DATABASE_MAX_IDLE_CONNS=${DB_MAX_IDLE_CONNS}
DATABASE_CONN_MAX_LIFETIME=${DB_CONN_MAX_LIFETIME}
